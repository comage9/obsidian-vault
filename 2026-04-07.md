# 2026-04-07

## ACP (Claude Code) 연결 시도
- 주현이 Claude Code ACP 페어링을 계속 요청
- acp.defaultAgent: claude-code 설정됨
- 페어링이 성공하지 못함 — 원인 파악 필요
- 주현의 의도: 앞으로 코딩 작업은 Claude Code로 진행
- 프론트엔드 DnD, 백엔드 sort_order 배포 등 대기 중인 작업 많다

## 백엔드 sort_order 변경사항 (GitHub에 push 완료, 서버 배포 미완료)
- models.py: sort_order IntegerField 추가
- views.py: _production_model_to_dict에 sortOrder 포함, bulk-reorder 엔드포인트 추가
- 서버에서 git pull + migrate 필요

## 프론트엔드 DnD 대기
- @dnd-kit/core, @dnd-kit/sortable 설치됨
- production-plan.tsx 수정 필요 (1400줄+)
- Claude Code 연결 후 작업 예정
# 2026-04-07

## ACP (Claude Code) 연결 시도
- 주현이 Claude Code ACP 페어링을 계속 요청
- acp.defaultAgent: claude-code 설정됨
- 페어링이 성공하지 못함 — 원인 파악 필요
- 주현의 의도: 앞으로 코딩 작업은 Claude Code로 진행
- 프론트엔드 DnD, 백엔드 sort_order 배포 등 대기 중인 작업 많다

## 백엔드 sort_order 변경사항 (GitHub에 push 완료, 서버 배포 미완료)
- models.py: sort_order IntegerField 추가
- views.py: _production_model_to_dict에 sortOrder 포함, bulk-reorder 엔드포인트 추가
- 서버에서 git pull + migrate 필요

## 프론트엔드 DnD 대기
- @dnd-kit/core, @dnd-kit/sortable 설치됨
- production-plan.tsx 수정 필요 (1400줄+)
- Claude Code 연결 후 작업 예정

## DnD 수정 작업 (직접 진행)
- Playwright 설치 및 테스트 자동화 구축 (`/usr/bin/google-chrome` 사용)
- **핵심 버그 발견**: 여러 SortableContext가 하나의 DndContext 안에 있어서, 드래그 시 다른 기계 그룹의 행으로 overId가 잘못 잡힘
- **해결책**: 단일 SortableContext로 통합 + handleDragEnd에서 JS로 같은 기계 검증
- IIFE 패턴 `(() => {...})()` 에서 esbuild 파싱 에러 반복 발생
- `useMemo`로 machineGroupEntries 분리 후 렌더에서 직접 사용하는 방식으로 변경 중
- Vite 빌드 에러, 괄호 불일치 등으로 여러 번 수정 시도

## 현재 상태 (18:19)
- production-plan.tsx에 esbuild 파싱 에러 있을 가능성 높음 (마지막 수정이 완료되지 않았을 수 있음)
- **GLM 5.1 주간/월간 한도 도달** → 16:47 reset 예정이었으나 계속 작업하면서 한도 초과 반복
- 주현이 "한도 도달 전에 다른 무료 모델로 전환해야 한다"고 지적
- **필요 조치**: 모델 한도 확인 + 대체 모델(OpenRouter 무료) 설정
- 프론트엔드 dev 서버가 5175 포트로 돌아가고 있었음 (5174 사용 중이라)
- Windows 프로덕션 서버 배포 아직 안 됨

## 주현 피드백
- DnD "왠만하면 동작은 한다"고 함 — 부분적으로 작동은 하는 상태
- 한도 초과 메시지에 당황 — 대체 모델 전환 요청
