# 2026-04-10 (금요일)

## AI 배송 예측 분석 영어+한글 혼용 문제 🔴 **아직 미해결**

### 시도한 해결책 (모두 실패)
1. **모델 변경**: `nvidia/nemotron-3-nano-30b-a3b` → `openrouter/google/gemma-4-26b-a4b:free`
   - 결과: Gemma도 thinking 모델이라 영어로 사고 과정 출력 → 같은 문제
2. **백엔드 프롬프트 강화** (`sales_api/views.py`): "한국어로만 답변, 사고 과정 출력 금지"
   - 결과: AI 모델이 프롬프트 무시
3. **프론트엔드 필터링** (`dashboard.js`) 3회 강화:
   - 1차: 한글 포함 줄만 유지 → 영어+한글 혼합 줄 통과
   - 2차: 한글 문자 > 영어 단어 수 && 영어 단어 < 5개 → 너무 강해서 아예 걸러짐
   - 3차: 서브에이전트가 수정 → 여전히 안 됨
   - 결과: 필터링이 아예 안 되거나 너무 강력해서 전체 삭제

### 현재 상태
- **API 직접 테스트** (`/api/ai/analyze`): ✅ 한국어만 정상 반환
- **실제 페이지** (`/delivery`): ❌ 영어 thinking 그대로 노출 ("We need to produce answer in Korean...")
- **원인**: AI 모델이 프롬프트 무시하고 thinking 과정을 응답에 포함. 프론트엔드 필터링 작동 안 함

### 다음 시도 필요한 것
- **백엔드 후처리**: 응답에서 한글만 추출해서 반환 (가장 확실한 방법)
- **모델 교체**: thinking 없는 비-thinking 모델로 교체 (`minimax/MiniMax-M2.7`)
- **OpenRouter API 파라미터**: `include_reasoning: false` 같은 옵션 확인

## 완료된 작업 (간단히)
- /delivery AI 분석 페이지 API 직접 테스트 결과: 한국어만 출력됨
- AI 분석 모델 변경: gemma-4-26b-a4b:free 적용 중

## VF 백엔드/프론트엔드 상태
- 백엔드: localhost:8000 (API)
- 프론트엔드: localhost:5174 (React)
- 테스트용 URL: http://220.121.225.76:5174/delivery
