# PBM140MW 재시도 금지 — 스킬 수정 기록 (2026-06-04)

**사건**: 6/3 PBM140MW 1~3호차 등록 시 1차/2차/3차 POST로 **총 6건 중복 INSERT** (사용자 KPP UI에서 발견)
**원인**: KPP `PBM140MW.save` API는 `flag:true` 반환하지만 **실제 DB INSERT 보장 안 함** + `PBM140MW.search`는 저장된 데이터를 못 가져옴 (0건 반환) → 거짓 보고로 재시도

## 스킬 수정 내역

### 1. `kpp-pallet-request/SKILL.md` (24,853 → 27,585 B, +2,732 B)

#### 추가된 섹션
- **"🚨 PBM140MW 등록 — 절대 재시도 금지 규칙"** (Pitfalls 다음, 변경이력 앞)
- 포함 내용:
  - 사건 기록 (6/3 6건 중복)
  - 3가지 오해 vs 정답 (검증 우선순위)
  - 사전 검증 (POST 전 search 0건 확인)
  - 사후 검증 (POST 후 UI 직접 확인)
  - 자동화 스크립트 사전/사후 검증 로직 (코드 예시)
  - "재시도해" 지시 시 3단계 안전장치
  - 변경 이력 (v5)

#### 정정된 기존 섹션 (6/3 → 6/4)
- L266: "🚨 `flag:true`는 실제 DB INSERT 보장 안 함" 추가
- L272: "`flag:true` + search 0건 ≠ 저장 안 됨" 명시
- L273: "PBM140MW는 중복 INSERT 가능" 경고
- L274~277: 검증 우선순위 (UI > flag > search)

### 2. `kpp_pbm140mw_register.py` (5,736 → 7,206 B, +1,470 B)

#### 추가된 검증 단계
- **[5] 사전 검증**: search로 6/3 등록 건수 확인 → 0건 아니면 POST 차단
- **[6] PBM140MW.save POST** (사전 검증 통과 시에만)
- **[7] 사후 검증**: search 응답 무시 + UI 확인 필수 + 예상 건수 계산

#### 안전장치 코드
```python
if pre_count > 0:
    print(f"[FAIL] 이미 {pre_count}건 등록됨 → POST 안 함 (중복 INSERT 방지)")
    print(f"[BLOCK] POST 차단. 사용자 KPP UI 확인 후 '계속' 입력 필요")
    raise SystemExit(0)
```

## 핵심 규칙 (한 줄)

**`flag:true` 단독으로 "저장됐다/안 됐다" 판단 절대 금지. KPP UI에서 직접 그리드 N건 카운트가 유일한 검증.**

## 적용 효과 (다음 6/4 등록 시)

1. 스크립트 실행 → 사전 search → **0건이면 진행**
2. 사전 search **> 0건이면 즉시 차단** (중복 방지)
3. save POST → flag 받음
4. 사후: 사용자에게 UI 확인 요청 메시지
5. 사용자 UI에서 N건 확인 → 진짜 검증

## 변경 이력

- 2026-06-04 (v5, PBM140MW 6/3 사건 기반): 재시도 금지 규칙 + 사전/사후 검증
  - 6/3 6건 중복 사건의 재발 방지
  - 자동화 스크립트에 안전장치 추가
  - 검증 우선순위 명시 (UI > flag > search)

## 관련 문서

- 스킬: `~/.hermes/skills/automation/kpp-pallet-request/SKILL.md`
- 스크립트: `~/.hermes/skills/automation/kpp-pallet-request/scripts/kpp_pbm140mw_register.py`
- 정본 동기화: `data1/hermes-backup/skills/automation/kpp-pallet-request/`
- 위키: `data1/hermes-backup/obsidian/06-Wiki-시스템/Wiki/물류/KPP/`
- 운영원칙: `workflow-discipline` + `agent-honesty-and-verification` + `verification-mandatory`
- SOUL.md: `~/.hermes/SOUL.md` (매 턴 자동 주입)
