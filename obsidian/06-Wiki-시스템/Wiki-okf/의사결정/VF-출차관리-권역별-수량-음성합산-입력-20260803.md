---
title: VF 출차관리 권역별 수량 음성 합산 입력 + LS/KPP 인쇄 — 요구사항 및 실행 매뉴얼
created: 2026-08-03
updated: 2026-08-03
type: 의사결정
status: 확정 (약어·저장경로 확정 2026-08-03)
신뢰도: REQUIREMENT
tags: [vf, departure, 권역, regions, 음성입력, 합산, 인쇄, LS, KPP, 저사양모델]
---

# VF 출차관리 권역별 수량 음성 합산 입력 + LS/KPP 인쇄

> 작성일: 2026-08-03
> 목적: **저사양 모델도 이 문서만 읽고 그대로 실행할 수 있게** 하는 실행 매뉴얼(Runbook)
> 대상: 텔레그램/채팅으로 사용자 말을 받아 에이전트가 대신 입력·인쇄

---

## 1. 사용자 요구사항 (정리)

**핵심:** 사용자가 **호차와 권역별 수량을 말하면**, 에이전트가 **파싱 → 합산 → 저장**.
인쇄(LS/KPP)도 사용자 말 한마디면 에이전트가 대신 실행.
**이 모든 것을 저사양(저비용) 모델도 이해하고 수행할 수 있어야 함.**

### 1.1 입력 시나리오
- 먼저 **호차**를 부른다 (예: "2호차")
- 이어서 **권역 + 수량**을 부른다 (예: "이스트 하나, 웨스트 하나, 미들 하나")
- 여러 번 부르면 **동일 권역은 기존 값에 합산** (덮어쓰기 ❌)
- 기본: 2호차에 총수량(plt)이 먼저 들어가는 구조. 지금은 출차카드 테스트 중이라 임의 입력.

### 1.2 합산 예시
| 호출 | 입력 | 결과 |
|:----:|:-----|:-----|
| 1회차 | "2호차에 이스트 하나, 웨스트 하나, 미들 하나" | EAST=1, WEST=1, MIDDLE=1 |
| 2회차 | "이스트 두 개, 웨스트 두 개, M 두 개" | +EAST=2, +WEST=2, +MIDDLE=2 |
| **최종** | — | **EAST=3, WEST=3, MIDDLE=3 (합계 9P)** |

### 1.3 권역 약어 매핑 (사용자 규칙)
| 사용자가 말하는 것 | 저장 코드 |
|:-----|:-----|
| 이스트 / EAST | `EAST` |
| 웨스트 / WEST | `WEST` |
| 미들 / M | `MIDDLE` |
| GMH / G | `GMH` (**'G' = GMH 확정, 2026-08-03 사용자**) |
| DGU / D | `DGU` |
| GZJ | `GWJ` |
| 야마토 / TDG 야마토 | `TW_YAMATO` |

> ✅ 약어 확정 (2026-08-03 사용자): **'G' = GMH**. 미들은 '미들' 또는 'M' 둘 다 `MIDDLE`.

---

## 2. 현재 시스템 현황 (실측, 2026-08-03)

### 2.1 이미 존재하는 것
- `ls_data.json` 각 차량 entry에 `regions` 객체 이미 존재:
```json
{
  "hoche": 2, "plate": "경기89바1454", "date": "2026-08-03",
  "regions": { "EAST": 0, "WEST": 0, "MIDDLE": 0, "GMH": 0, "DGU": 0, "GWJ": 0, "TW_YAMATO": 0 }
}
```
- 프론트 카드 UI에 권역별 입력 필드 7개 + **합계 자동 계산** + 서버 동기화 (`onVF67RegionChange`)
- 저장 API: `GET/POST /departure/api/ls-data` → `departure/data/ls_data_{date}.json`
- 인쇄 API: `/departure/api/print/{hoche}` (LS), `/departure/api/print-kpp/{hoche}` (KPP)

### 2.2 서버/경로 (중요)
| 항목 | 값 |
|:-----|:---|
| 프론트엔드 (Vite) | 포트 **5174** |
| 백엔드 (Django) | 포트 **5176** — 현재 **`E:\coding\VF-new` (원본)** 에서 실행 중 |
| 저장 파일 | `backend/departure/data/ls_data_{YYYY-MM-DD}.json` |
| 인쇄 프린터 | Canon G2010 series (서버 PC에서 무음 인쇄) |

---

## 3. 저사양 모델 실행 매뉴얼 — 권역 수량 입력 (Runbook)

**트리거 예시 문장:** "2호차 이스트 하나 웨스트 하나" / "1호차 M 둘, 야마토 셋" / "2호차에 G 하나 D 하나"

### Step 0. 날짜 결정
```
작업일 = 오늘. 단, 자정~06:00 사이라면 어제. (06:00 작업일 기준)
DATE = YYYY-MM-DD 형식
```

### Step 1. 사용자 말 파싱
1. **호차 추출**: "N호차"에서 숫자 N (없으면 이전 메시지의 호차 유지. 그래도 없으면 사용자에게 물어볼 것)
2. **권역+수량 추출**: §1.3 매핑표로 권역 코드 변환
3. **수량 한글 → 숫자**: 하나/한개=1, 둘/두개=2, 셋/세개=3, 넷=4, 다섯=5 … (아라비아 숫자면 그대로)
4. 파싱 결과 예: `{hoche: 2, adds: {EAST: 1, WEST: 1}}`
5. 못 알아들으면 **추측 금지** — 사용자에게 다시 물어볼 것

### Step 2. 현재 데이터 조회
```bash
curl -s "http://localhost:5176/departure/api/ls-data?date={DATE}"
```
- 응답 `data` 배열에서 `hoche == N` 인 차량 찾기 → 기존 `regions` 읽기 (없으면 전부 0)

### Step 3. 합산
```
새값 = 기존 regions + 파싱된 adds  (권역별 덧셈, 다른 권역은 그대로 유지)
```

### Step 4. 저장 (전체 배열 POST)
- Step 2에서 받은 `data` 배열 **전체**를 가져와서, 해당 호차의 `regions`만 새값으로 교체
- 다른 차량/필드는 절대 수정하지 않음
```bash
curl -s -X POST "http://localhost:5176/departure/api/ls-data?date={DATE}" \
  -H "Content-Type: application/json" \
  -d '<전체 배열 JSON>'
```
- ⚠️ **반드시 `?date={DATE}` 붙일 것** (안 붙이면 서버 고정 TODAY 날짜에 저장되는 버그 있음)

### Step 5. 검증 (필수 — POST ok:true만 믿지 말 것)
```bash
curl -s "http://localhost:5176/departure/api/ls-data?date={DATE}"
```
- 해당 호차 `regions` 값이 Step 3 결과와 일치하는지 확인
- 불일치 시: 저장 파일 `backend/departure/data/ls_data_{DATE}.json` 직접 확인 (서버 실행 폴더 주의 — 현재 원본 `E:\coding\VF-new`)

### Step 6. 보고 (짧게)
```
예: "2호차 저장 완료 — EAST 3 / WEST 3 / MIDDLE 3 (합계 9P)"
```

---

## 4. 저사양 모델 실행 매뉴얼 — LS 인쇄 (Runbook)

**트리거 예시:** "1호차 LS 인쇄" / "2호차 LS 찍어"

### 전제 조건
- 백엔드 5176 실행 중
- 해당 날짜 LS PDF 존재: `backend/departure/data/ls_pdfs/{DATE}/{plate}.pdf` (차량번호로 매칭)
- 봉인씰 번호가 필요하면 사용자가 불러줌 (좌익/우익/뒷문 3개)

### 실행
```bash
curl -s "http://localhost:5176/departure/api/print/{HOCHE}?plt={파렛트수}&date={DATE}" \
  --get \
  --data-urlencode "seal_leftWing={좌익씰}" \
  --data-urlencode "seal_rightWing={우익씰}" \
  --data-urlencode "seal_backDoor={뒷문씰}"
```
- `plt`: 파렛트 수량 (화면 입력값). 모르면 GET ls-data의 `plt` 값 사용
- 봉인씰 3개는 옵션 — 없으면 생략 가능 (생략 시 합성 없이 원본 인쇄)
- 서버가 알아서: 호차→차량번호 조회 → PDF 본문 매칭 → 봉인씰 합성 → Canon G2010 인쇄
- 응답: `{ok: true/false, hoche, results: [...]}` — `results` 배열 내용을 그대로 사용자에게 보고

### 실패 시
- `차량 정보 없음` → 해당 날짜/호차 데이터 없음. GET ls-data로 먼저 확인할 것
- PDF 없음 → `ls_pdfs/{DATE}/` 폴더에 차량번호.pdf 있는지 확인할 것

---

## 5. 저사양 모델 실행 매뉴얼 — KPP 인쇄 (Runbook)

**트리거 예시:** "2호차 KPP 인쇄" / "2호차 KPP 등록하고 찍어"

### ⚠️ 전제 조건 (LS 인쇄와 다름 — 반드시 확인)
1. **Chrome이 CDP 포트 9222로 실행 중**이어야 함: `curl -s http://localhost:9222/json/version` 로 확인
2. **WPPS 로그인** 되어 있어야 함 (세션 만료 시 로그인 페이지로 튐 → 사용자에게 로그인 요청)
3. **PBM140MW 탭이 열려 있어야** 함 (KPP 출하통보등록 페이지)

전제 확인 명령:
```bash
curl -s http://localhost:9222/json | grep -c PBM140   # 1 이상이면 탭 존재
```

### 실행
```bash
curl -s "http://localhost:5176/departure/api/print-kpp/{HOCHE}?plt={파렛트수}&date={DATE}"
```
- `plt` **필수** (0이거나 없으면 에러 반환 — 화면 입력값만 사용, 기본값 강제 없음)
- 서버가 알아서: 호차 차량정보 로드 → KPP 등록/갱신(기존 행 있으면 갱신) → EDI 전표 인쇄
- 응답 `results` 배열 그대로 보고

### 실패 시
- CDP 미연결 → Chrome 실행 필요 안내
- WPPS 로그인 페이지로 튐 → 사용자가 수동 로그인 후 재시도
- KPP 상태 확인 API: `GET /departure/api/kpp-check/{HOCHE}?date={DATE}`

---

## 6. 저사양 모델 공통 규칙

1. **추측 금지** — 못 알아들으면 물어볼 것. 숫자/호차/권역이 불명확하면 절대 임의 입력 금지
2. **검증 후 보고** — curl로 실제 응답 확인 전에는 "완료" 보고 금지
3. **날짜는 항상 명시** — `?date={YYYY-MM-DD}` (06:00 작업일 기준)
4. **서버 폴더 주의** — 현재 5176은 `E:\coding\VF-new` (원본) 실행 중. 파일 확인은 원본 폴더에서
5. **보고는 짧게** — 결과 값만. 예: "2호차 EAST 3/WEST 3/MIDDLE 3 합계 9P 저장 완료"

---

## 7. 제안/확정 필요 사항

| # | 항목 | 결정 | 상태 |
|:--|:-----|:-----|:----:|
| 1 | 약어 'G' | **G = GMH** (GZJ는 풀네임만 인정) | ✅ 확정 2026-08-03 |
| 2 | 미들 표기 | '미들' 또는 'M' 모두 MIDDLE | ✅ 확정 2026-08-03 |
| 3 | 저장 경로 | **`E:\coding\VF-new` 원본 경로에 저장** (현재 서버가 원본에서 실행 중이므로 그대로 유지 — 전환 작업 불필요) | ✅ 확정 2026-08-03 |
| 4 | 추가 UI 작업 | 불필요 — 기존 카드/합계/인쇄 API 그대로 활용 | ✅ |

---

## 관련 문서
- 스킬: `departure-dispatch-system`, `vf-new-workflow`, `kpp-pallet-management`
- 데이터: `backend/departure/data/ls_data_{date}.json`, `ls_pdfs/{date}/`
- API: `views.py` — `api_ls_data`(292), `api_print`(853), `api_print_kpp`(978), `api_kpp_check`(1114)
