# 제품배치도 4개 탭 동일 형식 + 제품 마스터 기준 (2026-08-18)

## 배경
- 사용자 지시: 배치/미배치/임시보관함/3개월 미출고 **모두 같은 형식**으로 — 분류별 그룹, 분류 클릭 시 세부 품목, **현재고 표시**.
- **제품목록의 기준 = 제품 마스터 관리** (`/api/master/specs`).

## 변경 내용
| 항목 | 변경 |
|:-----|:-----|
| 공통 컴포넌트 | `CategoryList` 신설 — 분류(대분류) 그룹 → 클릭 시 세부 품목 + `현재고 N` 표시 |
| 4개 탭 | 배치/미배치/임시보관함/3개월 미출고 전부 CategoryList 형식으로 통일 |
| 제품 마스터 | `masterMap` state — `/api/master/specs`의 **is_vf_item 875개** 로드 (name/lg/md/stock/barcode/loc/no3m) |
| 미배치 | 하드코딩(A_UNPLACED) → **마스터 기준** 197개 (배치 제외 + 미출고 제외) |
| 3개월 미출고 | **135개** (마스터 is_no_outbound_3m) — A_NO_OUTBOUND_3M 하드코딩은 fallback |
| 임시보관함 | 드래그 칩(드롭존) 유지 + 분류 그룹/현재고 리스트 추가 |
| no3mPnums | product_number 중복(102개)으로 masterMap이 덮어써지는 문제 → **별도 Set**으로 135 정확 유지 |

## 구현 포인트
- `MasterInfo` 타입 + `locToPnum()` (로케이션 → 제품번호 규칙 재사용)
- `masterMap` 구축: product_number + location 파생 pnum 양쪽 키 등록, **no3m 항목 우선 덮어쓰기**
- placedRows: 모든 동(data) 배치 품목 → 마스터 정보 우선, 하드코딩 fallback
- noOut3mRows/unplaced: 마스터 미로드 시 하드코딩 fallback (로딩 중 깜빡임 방지)

## 검증
- `tsc --noEmit` 0 에러
- 브라우저: 탭 4개 (배치/미배치 197/📦11/🚫135), 각 탭 분류 클릭 → `현재고` 표시 확인, console 0 errors
- 미배치 분류 예: 에센셜 66 · 북 트롤리 20 · 모던 플러스 14 · 레브 13 · 슬림서랍장 11

## 관련
- Git: `5167d06` (VF-.repo main)
- 스킬: `vf-product-display`
