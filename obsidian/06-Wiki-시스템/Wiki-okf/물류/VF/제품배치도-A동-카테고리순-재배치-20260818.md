# 제품배치도 A동 카테고리 순 재배치 + 3개월 미출고 탭 (2026-08-18)

## 배경
- 사용자 지시: A동에 있는 **칵투스 계열 + 데크 타입 계열은 임시(📦임시보관함)로 보내고**, 나머지 공간을 **상위권 모던(플러스) + 슬림 서랍장**으로 채움.
- 분류 순서 확정: **모던 플러스 → 슬림서랍장 → 로코스 → 나머지** (같은 분류끼리 진열, 뒤쪽으로 밀림).
- 3개월 미출고 품목은 **진열 대상에서 제외**하고 별도 탭으로 관리.

## 변경 내용
| 항목 | 변경 |
|:-----|:-----|
| A동 배치 | 132 → **109품목** (카테고리 순 재배치) |
| 칵투스/데크 | 16칸 제거 → **📦임시보관함 기본값 11품목** (staging-v2) |
| 3개월 미출고 | **135품목** → A_NO_OUTBOUND_3M, **🚫 3개월 미출고 탭 신설** |
| A_UNPLACED | 708 → **580** (미출고 128 제외) |
| A_PLACED_COUNT | 134 → 109 |
| 저장 버전 | rank-a-v15 → **rank-a-v16** (기존 localStorage 강제 교체) |

## 배치 규칙
- 카테고리 우선순위: `모던 플러스(0) → 슬림서랍장·슬림형(1) → 리빙박스 로코스(2) → 나머지(3)`
- 같은 카테고리 내에서는 기존 zone 순서 유지 (stable sort)
- 배치 중 미출고 7품목(242, 240, 224, 230, 216, 164, 250)도 배치에서 제거
- zone 블록(placement + CAT + MASTER_NAME + LG/MD + STOCK + BARCODE) 단위로 함께 이동

## 데이터 소스
- 3개월 미출고 판정: `/api/master/specs`의 **`is_no_outbound_3m`** 필드 (645품목 전체 중 A동 대상 135)
- 배치 데이터: `frontend/client/src/pages/product-display-a-data.ts`

## 검증
- `tsc --noEmit` 0 에러
- 브라우저: A동 109칸, 탭 4개(배치/미배치 211/📦임시보관함 11/🚫 3개월 미출고 135), console 0 errors
- 배치 ∩ 미출고 = ∅, 미배치 ∩ 미출고 = ∅

## 관련
- Git: `98279f8` (VF-.repo main)
- 스킬: `vf-product-display` (A동 레이아웃 SoT)
