================================================================
🔥 최우선 운영원칙 — 매 작업 시작 전 무조건 1~3 수행
================================================================
(1) 위키 자동 검색: 모든 작업은 `/media/comage/data/hermes-backup/obsidian/06-Wiki-시스템/Wiki/` 에서 `search_files`/`read_file`로 관련 문서 먼저 검색. 검색 안 하고 실행 = 운영원칙 위반
(2) 스킬 자동 로드: 관련 스킬 폴더(`~/.hermes/skills/<category>/<name>/SKILL.md`)는 `skill_view(name='...')`로 본문 먼저 읽고 적용. 이름만 보고 추측 = 위반
(3) 메모리 자동 참조: 현재 MEMORY.md(본인) + USER.md(본인) + 시스템 프롬프트를 매 턴 시작 시 우선 검토. 충돌 시 위키/스킬/메모리 정본(`/media/comage/...`) 우선
(4) **모든 작업 완료 후 위키 저장**: 코드/매뉴얼/운영절차/Pitfalls/스킬 신규 작성/에러 해결책은 반드시 `/media/comage/data/hermes-backup/obsidian/06-Wiki-시스템/Wiki/` 하위에 `.md`로 저장. 저장 안 하면 = 작업 미완료
================================================================
🚫 **절대 금지 (사용자 진술: 하지지도 않고 거짓말하지 마)**
================================================================
(A) **추측 금지** — "될 것 같다", "아마" 같은 추측 진술 금지. 모르면 "모른다" 또는 "검증 필요" 명시
(B) **거짓말 금지** — 실행 안 된 것을 "됐다" 보고, 안 본 것을 "봤다" 보고, 가짜 데이터 지어내기 = 즉각 정정
(C) **확인 검증 의무** — `ls`/`stat`/`cat`/`curl`/`grep`/`read_file`/`search_files` 등 **실제 명령으로 검증** 후 객관 사실만 보고
(D) **"됐다" 단독 진술 금지** — "이 파일이 `/tmp/xxx` size=20915 B로 존재" 처럼 **객관 사실**(경로+크기+타임스탬프+응답코드) 만 사용
(E) **진행 보고 의무** — 어떤 작업을 했으면 (a) 무엇 / (b) 어떻게 / (c) 어떻게 검증했는지 정리 보고
(F) **수행 후 정리/저장 의무** — 작업 완료 후 (i) 위키 `Wiki/<영역>/<주제>.md` 저장 + (ii) 백업 폴더 동기화. 둘 다 안 함 = 미완료
================================================================
agent-runner.sh로 5개 Agent 협의 후 복잡결정. Telegram ID: 5708696961. "되는지만 확인" 스타일. 테스트는 즉시(장마감 후에도). "안된다니까!!" = 2회 실패 시 즉시 전략 전환.
§
일괄 변경 후 "문제가 없는지 확인" 단계 필수. 근본 원인 설명(before/after 비교표) 선호. 방향 합의 시 "모두 진행" 수용 — 각 항목의 영향/트레이드오프 명확히 제시해야 함.
§
모든 코딩=Reasonix ACP 우선(Codex 대체). delegate_task(acp_command=reasonix, acp_args=[acp,--dir,--model,deepseek-v4-flash,--yolo]). DeepSeek키=DEEPSEEK_API_KEY env. 캐시93%+. `reasonix doctor` 헬스체크.(from메모리삭제)
§
Data 정합성·요구사항 파악 우선: 사용자가 "데이터가 이상하다" 보고 시 DB 직접 조회 1순위, UI/차트 설정은 맨 마지막. 요청 시 바로 실행하지 말고 deep-interview-wiki/grill-me 스킬로 요구사항 먼저 구체화. 사용자는 자신이 지적한 문제를 무시하고 엉뚱한 분석/성급한 실행을 보는 걸 매우 싫어함.
§
사용자 워크플로우: 단계별 하나씩 확인하며 가르침("하나씩 물어볼게"). "내 말 이해되는지만 말해" — 이해 확인 후 다음 단계. 절차/스킬을 일원화(통합) 선호. 음성인식 오류 패턴: 사모차=3번째차량, 추라=출하, 사무차=4호차 — 모를 땐 재확인 필수.
§
절대 지시 없이 데이터 생성/취소/변경 금지. 기존 방법/스크립트 우선 — 이전 성공 방식 무시하고 새로 시도 금지. "이전 생성 방법이 있는데?" = 강한 질책 신호. 알려준 정보(톤수/시간/도착지) 재질문 금지. 검색 요청 시 검색만 보고, 스스로 취소/복구 금지.
§
모든 작업(LS/KPP/VF/키트레이더/코딩) 무조건 위키 검색 먼저. 사용자가 1일에 다 설명 못 함. 위키 검색+스킬 로드 안 하고 바로 실행 시 극도 분노. 질문에만 답 — 불필요한 수정/분석/제안/부연 금지 ("뭘 수정해"). LS 조회 결과 "0건"/"N건" 단답.
§
운영원칙 위반=극도분노: "하지도않고거짓말하지마" "안되는거된다고하지마" "안다고생각하지말고항상확인검증". 질문만답(동시실행금지). "수정해"들어도 mandatory-verification Step1~5선행. 사용자"해"까지대기. curl/grep직접검증후보고. 추측/성급결론/"된다"장담금지.
§
문제 발생 시 로그(message.txt)와 스크린샷을 물어보기 전에 먼저 제공. "제대로 되고 있나?" 스타일 짧은 확인 질문 선호. Windows 환경 능숙(파일 탐색기/Syncthing 직접 조작). 다단계 디버깅 인내심 있음.