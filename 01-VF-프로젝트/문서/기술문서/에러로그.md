# 에러 로그

## 반복 발생 에러 및 해결 방법

### [2026-04-13] Claude Code 모델 접근 불가
- **에러:** `There's an issue with the selected model (qwen/qwen3.6-plus:free)`
- **원인:** OpenRouter 무료 모델 접근 불가
- **해결:** Z.AI API (GLM-4.7)로 변경
- **학습:** 모델 접근 불가 시 자동 전환 필요

### [2026-04-13] exec 보안 정책
- **에러:** `complex interpreter invocation detected; refusing to run`
- **원인:** `source` + `&&` 조합이 보안 정책에 걸림
- **해결:** 가상환경 파이썬 경로를 직접 지정
- **학습:** `/path/to/.venv/bin/python manage.py [command]` 형식 사용

### [2026-04-13] Windows 서버 sort_order 에러
- **에러:** `OperationalError: no such column: production_logs.sort_order`
- **원인:** Windows 서버 DB에 migration 미반영
- **해결:** 서버에서 `git pull` + `python manage.py migrate` 필요
- **학습:** 로컬과 서버의 migration 동기화 확인 필수

### [2026-04-07] IIFE → esbuild 파싱 에러
- **에러:** esbuild 빌드 실패
- **원인:** `(() => {...})()` IIFE 패턴
- **해결:** `useCallback`으로 분리
- **학습:** IIFE 패턴 사용 금지 → RULES/code.md에 반영

---
*이 파일은 같은 에러가 2회 이상 발생하면 자동화 도구로 전환 (2회 규칙)*
